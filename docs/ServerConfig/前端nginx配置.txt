让前端路由管理页面，并正向代理请求到后端。

server
{

    // ......

    location / {
        try_files $uri $uri/ @router;
        index index.html index.htm;
    }
    location @router {
        rewrite ^.*$ /index.html last;
    }
    location ~ /api/* {
      proxy_pass http://gallery-system.pinkcandy.top;
      rewrite ^/api/(.*)$ /$1 break;
    }
}
